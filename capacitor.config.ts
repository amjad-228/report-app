import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.reports',
  appName: 'نظام إدارة التقارير',
  webDir: 'out'
};

export default config;
